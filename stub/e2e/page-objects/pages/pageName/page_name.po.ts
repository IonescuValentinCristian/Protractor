// import {By, element} from 'protractor';

export class Octa2 {
	
	// static readonly CONST = "";
	
	/* Forms */
	
	/* Inputs */
    
	/* Buttons */
	
	/* Others */		    

	// getter sample
	/*
	static get newMessageBodyArea() {		
		return element(By.xpath('//div[@class="Am Al editable LW-avf"][@aria-label="Message Body"]'));
	}
	*/

	// method with params
	/*
	static newMessageBodyArea(name: string) {		
		return element(By.xpath('//div[@class="Am Al editable LW-avf"][@aria-label="' + name + '"]'));
	}
	*/
}
